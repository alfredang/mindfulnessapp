import SwiftUI

enum Theme {
    static let header = Color(red: 0.18, green: 0.48, blue: 0.60)
    static let background = Color(red: 0.04, green: 0.14, blue: 0.18)
    static let surface = Color.white.opacity(0.10)
    static let ink = Color.white
    static let mutedInk = Color.white.opacity(0.72)
    static let control = Color.white
    static let progress = Color.white.opacity(0.95)
    static let progressTrack = Color.white.opacity(0.14)
}
